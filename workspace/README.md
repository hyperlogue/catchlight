# Workspace

Scratch directory for intermediate files generated during AI-assisted
workflows, and for trees removed from git that we may want as a
reference when rebuilding them.

Files here are **not committed to git** but persist on disk so humans
can inspect them between sessions. Use this instead of `/tmp` for
anything you may want to review later.
